// let Arr = ['adil', 'adilc', 'adil c', 'a d i l c']
// console.log(Arr.sort((a, b) => a.length - b.length));
// let e = Arr.reduce((curr, currlength) => curr.length > currlength.length ? curr : currlength, '')
// console.log(e);


function hello(){
    return 
}

console.log(typeof hello);